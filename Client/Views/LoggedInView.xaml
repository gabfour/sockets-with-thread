<Page x:Class="Client.Views.LoggedInView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views"
      mc:Ignorable="d" 
      WindowHeight="400" WindowWidth="600"
      d:DesignHeight="400" d:DesignWidth="600"
      Title="LoggedInView" Background="White">
      
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Column="0" Grid.Row="0" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="{Binding Logs, FallbackValue='Logs'}" VerticalAlignment="Stretch" IsReadOnly="True"/>
        <Button Grid.Column="1" Grid.Row="0" Content="Disconnect" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding OnDisconnectClickedCommand}"/>
        <TextBox Grid.Column="0" Grid.Row="1" Text="{Binding Command, FallbackValue='Command'}"/>
        <Button Grid.Column="1" Grid.Row="1" Content="OK" Command="{Binding OnCommandSendClickedCommand}"/>
    </Grid>
</Page>
